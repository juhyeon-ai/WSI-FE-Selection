-r requirements.txt
Pillow>=10,<12
openslide-python>=1.3,<2
openslide-bin>=4.0,<5
opencv-python>=4.8,<5
# Install a compatible torch/torchvision pair first; see README.
# TRIDENT and SAM 1 are installed separately from pinned local clones.
